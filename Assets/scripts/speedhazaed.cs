using UnityEngine;

public class speedhazaed : MonoBehaviour
{
    public DungeonRaiderPlayer player;
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
         
    }

    
}
